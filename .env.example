# Stripe
STRIPE_SECRET_KEY=sk_test_TODO
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_TODO

# Stripe price IDs (one per tier). Create the products + prices in Stripe
# Dashboard, then paste the price IDs here. These are unused today because
# payment amounts are taken from lib/products.ts, but keeping them documented
# so switching to price-ID-based checkout is a one-line change.
STRIPE_PRICE_SNP=price_TODO
STRIPE_PRICE_SNP_IMPUTED=price_TODO
STRIPE_PRICE_WGS_30X=price_TODO
STRIPE_PRICE_WGS_100X=price_TODO

# Supabase — used by /api/save-order to persist orders
NEXT_PUBLIC_SUPABASE_URL=https://TODO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=TODO
SUPABASE_SERVICE_ROLE_KEY=TODO

# Resend — transactional confirmation emails
RESEND_API_KEY=re_TODO
RESEND_FROM=contact@genome.computer
